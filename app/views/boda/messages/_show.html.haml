.container
  %table#message-table
    %thead
      %tr
        %th Mensaje
        %th Usuario
        %th{:colspan => "3"}
    %tbody
      - Message.all.each do |msg|
        %tr
          %td= msg.body
          %td= User.find(msg.user_id).name
